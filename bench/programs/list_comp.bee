fn run(n) {
  let xs = []
  for (let i = 0; i < 2000; i = i + 1) { xs.push(i) }
  let total = 0
  for (let r = 0; r < n; r = r + 1) { total = total + len([x * 2 for x in xs if x % 3 == 0]) }
  return total
}
print(run(600))
