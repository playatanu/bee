# Bee-level throw/catch, which remains a real C++ exception.
fn risky(i) { if (i % 7 == 0) { throw "seven" } return i }
fn run(n) {
  let s = 0
  for (let i = 0; i < n; i = i + 1) {
    try { s = s + risky(i) } catch (e) { s = s + 1 }
  }
  return s
}
print(run(200000))
