# Writing back into a list: index-set plus compound assignment.
fn run(n) {
  let xs = []
  for (let i = 0; i < n; i = i + 1) { xs.push(0) }
  for (let r = 0; r < 3000; r = r + 1) {
    for (let i = 0; i < n; i = i + 1) { xs[i] += 1 }
  }
  return xs[0]
}
print(run(2000))
